  <ul style="background-color: #eeeeec;color:#2e3436;">
    <% @people.each do |person| %>
    <li onclick="submit_add_form('<%=person.id%>')" style="border-bottom:1px dashed #fcaf3e; border-top:1px dashed #fcaf3e;">
		<span class="show_this"><%=person.name%></span>
	</li>
    <% end %>
  </ul>

